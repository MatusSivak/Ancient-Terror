Built-in image generation tool; final chroma-key source prompt:

Use case: precise-object-edit. Input is the approved Ancient Terror antique ivory dice sprite sheet. Change ONLY the background. Replace the gray-white checkerboard with a perfectly uniform flat pure magenta background RGB(255,0,255), #FF00FF, everywhere outside the dice. This is a chroma-key source for lossless game-asset export. No checkerboard, no background shadow, no gradient. Keep every single die, every pip, every die orientation, its exact center, its exact size and all current shading and ivory surface texture absolutely unchanged. Preserve the exact 16 column by 9 row layout, top and bottom row each have only first cell occupied, middle seven rows all 16 cells occupied, 114 sprites total. No extra dice, text or border. Preserve canvas aspect ratio. High quality PNG.
